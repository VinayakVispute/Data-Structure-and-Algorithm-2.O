#include <iostream>
#include <algorithm>

using namespace std;

void returnPermutation(string str, int index, string output)
{
    if (index >= str.length())
    {
        cout << endl < str;
    }
 swap   
}
void solution(string str)
{
    for (int i = 0; i < str.length(); i++)
    {
        swap(str[0], str[i]);
        returnPermutation(str, 1, "")
    }
}

int main()
{
    string str;
    cin >> str;

    solution(str);
}